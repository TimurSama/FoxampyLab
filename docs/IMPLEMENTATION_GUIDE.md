# Инструкция по обновлению дизайна и функционала

## 🎯 Что было сделано

### 1. ✅ Главная страница - Галерея
- **Удалены**: 2 статичных кейса, текстовый блок описания
- **Добавлена**: интерактивная карусель с 6 реальными элементами
  - Фото-коллажи из `/gallery/` и `/architecture/`
  - Проекты: Web3 Bank, Mail Services, IoT Ecosystem  
  - Видео-превью: CGI 3D Cinema, Advertising Showreel
- **Улучшения**: крупные кнопки, анимации, адаптивность

### 2. ✅ Страница услуг - Улучшенные карточки
- **Анимации**: spring-эффекты при открытии модального окна
- **Новые услуги**: Фэшн и архитектура, Геймдев и геймификация
- **Обновления**: дизайн → дизайн и брендинг, улучшен маркетинг
- **Desktop**: модальные окна поверх сетки без смещения
- **Mobile**: сохранено поведение внутри карточек

### 3. ✅ Главная страница - Секция "Мультидисциплинарный синтез"
- **Замена**: TerrainGrid → полноценная витрина услуг
- **9 карточек**: все основные направления с фичами
- **Функциональность**: ссылки "Подробнее в услугах" на каждую услугу
- **Сохранение**: фоновая визуализация для глубины

### 4. ✅ Фоны - 8 интерактивных вариантов
- **Фикс**: корректное переключение между вариантами
- **Интерактивность**: реакция на движение мыши
- **Типы**: Thick Ink, Droplets, Waves, Swirls, Mist, Streams, Crystals, Deep Ocean
- **Демонстрация**: `/backgrounds` с переключением и превью

## 🔧 Технические решения

### Анимация модальных окон
```typescript
// Было: height: 'auto' (смещало сетку)
// Стало: fixed modal position + backdrop
<motion.div
  initial={{ opacity: 0, scale: 0.7, y: 50 }}
  animate={{ opacity: 1, scale: 1, y: 0 }}
  exit={{ opacity: 0, scale: 0.9, y: -50 }}
  transition={{ type: "spring", stiffness: 300, damping: 30 }}
  className="fixed inset-0 z-40 flex items-center justify-center"
>
```

### Динамическая смена Canvas компонентов
```typescript
// Ключ: использовать key для перерендера
<Canvas key={selectedModule.id}>
  <Suspense>
    <BackgroundComponent />
  </Suspense>
</Canvas>
```

### Реальный контент в карусели
```typescript
// Было: вымышленные проекты
// Стало: реальные данные из галереи
const carouselItems = [
  // Фото из /gallery/ и /architecture/
  // Видео из /media/  
  // Проекты с технологиями и описаниями
];
```

## 🎨 Дизайн-решения

### Цветовая палитра
- Сохранены оригинальные цвета сайта
- Ink-черная палитра для фонов
- Чистые переходы между состояниями

### Типографика
- Моноширинные шрифты везде
- Правильная иерархия размеров
- Улучшенная читаемость на всех устройствах

### Интерактивность
- Hover-эффекты только на десктопе
- Touch-оптимизация для мобильных
- Плавные spring-анимации

## 📱 Адаптивность

### Mobile (<768px)
- Карусель: 4 миниатюры, крупные кнопки
- Услуги: 1 колонка, внутри карточек
- Фоны: упрощенное управление

### Tablet (768px-1024px)
- Карусель: 6-8 миниатюр
- Услуги: 2 колонки, модальные окна
- Фоны: полный функционал

### Desktop (>1024px)
- Карусель: 8 миниатюр, hover-эффекты
- Услуги: 3 колонки, overlay модали
- Фоны: все эффекты и интерактивность

## 🚀 Как повторить

### 1. Структура компонентов
```
components/
├── sections/
│   ├── GalleryCarousel.tsx      # Карусель с реальным контентом
│   └── FAQSection.tsx         # FAQ с фильтрами
├── backgrounds/
│   ├── InkFluid1-8.tsx        # 8 вариантов фонов
│   └── InkFluidBackgroundCanvas.tsx
└── visuals/
    ├── InteractiveOilBackground.tsx
    └── SmartOilBackground.tsx
```

### 2. Тип данных для карусели
```typescript
interface CarouselItem {
  id: string;
  type: 'project' | 'photo' | 'design' | 'video';
  title: string;
  description: string;
  images?: string[];     // Для коллажей
  image?: string;       // Для проектов
  videoSrc?: string;   // Для видео
  technologies?: string[];
}
```

### 3. Spring-анимации для модалок
```typescript
const modalVariants = {
  initial: { opacity: 0, scale: 0.7 },
  animate: { opacity: 1, scale: 1 },
  exit: { opacity: 0, scale: 0.9 },
  transition: {
    type: "spring",
    stiffness: 300,
    damping: 30,
    mass: 0.8
  }
};
```

### 4. Canvas компоненты
```typescript
export default function InkFluid1() {
  const uniforms = useMemo(() => ({
    uTime: { value: 0 },
    uMouse: { value: new THREE.Vector2() }
  }), []);

  useFrame((state) => {
    uniforms.uTime.value = state.clock.elapsedTime;
    // Глобальная интерактивность с мышью
    if (state.pointer) {
      uniforms.uMouse.value.set(
        (state.pointer.x + 1) * 0.5,
        (1 - state.pointer.y) * 0.5
      );
    }
  });
}
```

## 📊 Результат

- ✅ **Главная**: карусель с 6 реальными работами
- ✅ **Услуги**: 9 направлений с улучшенными анимациями
- ✅ **Фоны**: 8 интерактивных чернильных эффектов
- ✅ **Дизайн**: консистентный с сохранением стиля
- ✅ **GitHub**: все изменения в репозитории

## 🎭 Ключевые фичи

1. **Интерактивная карусель** - показывает реальные работы из галереи
2. **Spring-анимации** - плавные открытия карточек услуг
3. **8 фонов** - разные эффекты чернильной жидкости
4. **Адаптивность** - корректная работа на всех устройствах
5. **Реальный контент** - без заглушек и вымышленных проектов

Все готово и протестировано! ✨